<% if @restaurants.empty? %>
  <p> No restaurants found </p>
<% else %>
  <% @restaurants.each do |restaurant| %>
    <%= link_to restaurant.name, restaurant %>:
    <%= restaurant.rating %> stars <p>
  <% end %>
<% end %>

<%= link_to 'Add Restaurant', new_restaurant_path %>
